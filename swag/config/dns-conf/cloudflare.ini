# Cloudflare API credentials for SWAG DNS validation
# 
# To get your Cloudflare API token:
# 1. Go to https://dash.cloudflare.com/profile/api-tokens
# 2. Create a new token with "Edit zone DNS" permissions
# 3. Select your domain (albizblog.online)
# 4. Copy the token and paste it below

# Comment out the old email/key method (if present)
# dns_cloudflare_email = your-email@example.com
# dns_cloudflare_api_key = your-api-key

# Use the new API token method (recommended)
dns_cloudflare_api_token = fNDzHyWd9zndQMjghXypW4bX6ZCGowsaof3q30cH

# Note: Replace YOUR_CLOUDFLARE_API_TOKEN_HERE with your actual Cloudflare API token
# The token should have permissions to edit DNS for albizblog.online 